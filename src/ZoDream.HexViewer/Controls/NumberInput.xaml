<UserControl x:Class="ZoDream.HexViewer.Controls.NumberInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZoDream.HexViewer.Controls"
             Width="100"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="100">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="{DynamicResource IconFontFamily}"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="ValueTb" Text="0" MaxLines="1" TextAlignment="Center" Grid.RowSpan="2" TextChanged="ValueTb_TextChanged"/>
        <Button x:Name="PlusBtn" Content="&#xE010;" Click="PlusBtn_Click" Grid.Column="1"/>
        <Button x:Name="MinusBtn" Content="&#xE011;" Click="MinusBtn_Click" Grid.Column="1" Grid.Row="1"/>
    </Grid>
</UserControl>
